# 2026-07-23

## 项目架构总结（应用户要求）
- 梳理了 FAOS TradingAgents 架构：Python/FastAPI 后端(:8001) + React/Vite 前端(:5173)，asyncio EventBus 事件驱动。
- 核心链路：/api/plan/chat → PlannerPipeline(LLM 对话式规划, clarify/ready) → TaskRuntime.submit_task → TaskSubmitted → Planner 映射 Workflow→DAG → ExecutionPlanGenerated → ExecutionEngine 按依赖调度 Skill → NodeCompleted/TaskCompleted → WS /ws/events 广播前端。
- AnalyzeStockWorkflow 节点：FetchData+FetchNews(并行) → Analyze(4视角) → Discuss(Bull/Bear→ResearchManager→CRO→PM) → Decision(PolicyEngine 打分)。
- 数据经 ExecutionContext 累积；ProviderService 按优先级路由(yfinance/WebSearch 100 > mock 10)；SQLite 存历史，accuracy_backtester + experience_service 形成自优化闭环。
- 产出两张 SVG 架构/数据流图（内联可视化）。
